# Base URL of the Site Audit FastAPI service.
# Defaults to the deployed Render service when unset.
API_BASE_URL=https://site-audit-vil4.onrender.com

# Optional. Set only if the API enforces AUDIT_API_KEY; sent server-side as x-api-key.
# AUDIT_API_KEY=
