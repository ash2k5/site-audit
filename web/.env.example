# Base URL of the Site Audit API. The browser calls it directly, so this is
# inlined into the client bundle at build time. Defaults to the deployed service.
NEXT_PUBLIC_API_BASE_URL=https://site-audit-vil4.onrender.com
